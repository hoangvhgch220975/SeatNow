/**
 * bookingExpire.job.js - scheduled job to expire old pending bookings (placeholder)
 */
module.exports = async function expireBookings() {
  // Implement job logic: find expired pending bookings, mark as expired, notify
  console.log('bookingExpire job placeholder running');
};
