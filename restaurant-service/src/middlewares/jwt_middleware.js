/**
 * JWT middleware placeholder
 * Replace with real JWT verification (e.g., jsonwebtoken) when needed.
 */
module.exports = function jwtMiddleware(req, res, next) {
  // noop placeholder: attach req.user when implementing
  next();
};
