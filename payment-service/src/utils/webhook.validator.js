/**
 * webhook.validator.js - validate provider webhook signatures (placeholder)
 */
function validateSignature(req) {
  // Implement per-provider rules
  return true;
}

module.exports = { validateSignature };
